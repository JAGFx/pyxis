<twig:Liste:Item>
    <div class="flex justify-between items-center">
        <span>{{ account.name }}</span>
        <a data-turbo="true" href="{{ path('front_account_cash_flow', {'id': account.id}) }}" class="text-muted flex items-center">
            <small>
                <span>{{ 'account.list.actions.see_cash_flows' | trans }}</span>
                <i class="fa-solid fa-scale-balanced"></i>
            </small>
        </a>
    </div>

    <twig:block name="detail">
        <turbo-frame id="account_list_item__cash_flow_by_account_{{ account.id }}" class="space-y-2"></turbo-frame>
    </twig:block>
</twig:Liste:Item>

{#    <twig:ItemDropdown>#}
{#        <li>#}
{#            <a class="dropdown-item" href="{{ path('back_account_edit', {'id': account.id}) }}">Modifier</a>#}
{#        </li>#}
{#        {% if account.enable %}#}
{#            {% if is_granted(constant('App\\Domain\\Account\\Security\\AccountVoter::DISABLE'), account) %}#}
{#                <li>#}
{#                    <a class="dropdown-item" data-turbo="true"#}
{#                       href="{{ path('front_account_toggle', {'id': account.id}) }}">Désactiver</a>#}
{#                </li>#}
{#            {% endif %}#}
{#        {% else %}#}
{#            <li>#}
{#                <a class="dropdown-item" data-turbo="true"#}
{#                   href="{{ path('front_account_toggle', {'id': account.id}) }}">Activer</a>#}
{#            </li>#}
{#        {% endif %}#}
{#    </twig:ItemDropdown>#}

