{% extends 'base.html.twig' %}

{% block body %}
    <twig:PageTitle :icon="icons.transfer" label="{{ 'transfer.title' | trans }}" />

    <twig:Card color="acrylic" size="lg" class="shadow-lg">
        {{ form_start(form) }}
        {{ form_errors(form) }}

        {{ form_row(form.account) }}
        {{ form_row(form.budgetSource) }}
        {{ form_row(form.budgetTarget) }}
        {{ form_row(form.amount) }}

        <div class="pt-4 pb-1 flex justify-end">
            <twig:Button type="submit" :icon="icons.save" label="{{ 'actions.save' | trans(domain: 'interface') }}" dir="right" size="lg"/>
        </div>

        {{ form_end(form) }}
    </twig:Card>
{% endblock %}